# ТЗ.md — Техническое задание WinPlax (v0.2 + дополнения 2026-09-20/21)

> Актуально на 2026-09-21 04:50, ветка `arena/01a0bd65-winplax`.
> Базовый документ — `README.md` v0.2, ниже — консолидация всех принятых правок.

## 1. Общее назначение

Параметрический генератор оконного блока с экспортом в **DXF R2013 (AC1027) `ANSI_1251/cp1251`** и далее **DWG ACAD2013** через **ODA File Converter**, без зависимости от AutoCAD. Строго **ГОСТ 21.501 / 23166 / 2.303**.

## 2. Состав поставки и команды

- `window_export.py` — одиночное окно (`params.json` + `profiles.json` → `output/<window_name>.dxf/.dwg` + `*_INSIDE` если `view=OUTSIDE`) **и** сборка всех примеров (`output/Все_примеры.dxf → .dwg`).
- `generate_examples.py` — 12 примеров (6 `Снаружи` + 6 `Изнутри`, 3 системы) в один файл с шагом `3500×3500`, `output/Все_примеры.dxf`.
- `test_window_export.py` — 17 тестов (аудит, замыкание, слои/стили, INSIDE/OUTSIDE, сплошной импост, штапик, заполнение).
- `params.json` — эталон `ОК-1 1500×1500 3×2` (`system ABSTRACT`, `view OUTSIDE`, `mullion auto`, `bead 25`).
- `profiles.json` — 3 системы: `ABSTRACT_60_80_25` / `REHAU_GRAZIO_70` / `EXPROF_PROFECTA_S571_70`.
- `Шаблон.dxf` — пользовательский шаблон (приоритет).

```bash
python window_export.py [params.json] [-o output/ОК-1.dxf] [-t Шаблон.dxf]
python generate_examples.py [-t Шаблон.dxf] [-o output/Все_примеры.dxf]
python -m unittest -v
```

## 3. Входные параметры (`params.json` v0.2+)

```json
{
  "version":"0.2",
  "window_name":"ОК-1",
  "system":"ABSTRACT_60_80_25",
  "opening":{"width":1500,"height":1500,"seam":30},
  "frame":{"face_width":60,"face_height":60},
  "mullion":{"width":80,"height":80,"continuous":"auto"},
  "bead":{"width":25},
  "sash":{"overlap":15,"profile_width":80},
  "sill":{"on":true,"height":30},
  "addons":{"left":0,"right":0,"top":0},
  "cols":3,"rows":2,
  "cells":[
    {"row":1,"col":1,"sash_type":"TURN"},
    {"row":1,"col":2,"sash_type":"TURN_TILT"},
    {"row":1,"col":3,"sash_type":"FIX"},
    {"row":2,"col":1,"sash_type":"TILT"},
    {"row":2,"col":2,"sash_type":"FIX"},
    {"row":2,"col":3,"sash_type":"FIX"}
  ],
  "metadata":{
    "object":"Тестовый объект",
    "window_name":"ОК-1",
    "color_out":"RAL 8017",
    "color_in":"RAL 9016",
    "glazing":32,
    "quantity":"1 шт."
  },
  "view":"OUTSIDE"
}
```

- `system` — переключатель оконной системы (`ABSTRACT_60_80_25` учебная 60/80/25/15, `REHAU_GRAZIO_70` 63/76/73/14.5/5, `EXPROF_PROFECTA_S571_70` 60/74/74/20/8). Если `frame/mullion/sash/bead` не заданы в `params` — берутся из `profiles.json`; явные значения в `params` приоритетны. Ошибка если `system` не найдена.
- `opening.seam` — монтажный зазор `S` (≥0).
- `frame.face_*` — видимая ширина коробки; проверяётся `2·(S+FW) < OW` и аналогично по `H`. Fallback из системы.
- `mullion.width/height` — импост; **`mullion.continuous` — сплошной импост** (`auto` по умолчанию, `vertical`, `horizontal`, алиасы `v/h`, `вертикаль/горизонталь`, `continuous_impost`).
- **`bead` / `shtapik` / `bead_width` / `shtapik_width` — штапик, единый для рамы/импостов/створок, ширина `bw` мм, по умолчанию из системы** (`ABSTRACT 25`, `GRAZIO 14.5 [6.5/14.5]`, `Profecta S358 20`) или `25.0`, `≥0`, `≤ FW`.
- `sash.overlap` — наплав, `profile_width` — брусок створки (80 по умолчанию) → внутренний свет `in = outer - 2·SW`. Fallback из системы (`REHAU 5`, `Profecta 8`).
- `sill` — подставочник `y=S .. S+SH`, рама над ним `y=S+SH`; доборы `left/right/top` уменьшают раму: `frame_left=S+left`, `frame_right=OW-S-right`, `frame_top=OH-S-top`.
- `cols/rows` ≥1, `cells` — карта `FIX/TURN/TILT/TURN_TILT`.
- `metadata.quantity` — по умолчанию `1 шт.`.
- `view` — `OUTSIDE` (наплав скрыт) / `INSIDE` (зеркало `X→OW-X`, наплав виден, обрезает раму).

`profiles.json`:
```json
{
  "ABSTRACT_60_80_25": {"frame":60,"mullion":80,"sash":80,"bead":25,"overlap":15,"depth":70},
  "REHAU_GRAZIO_70": {"frame":63,"mullion":76,"sash":73,"bead":[6.5,14.5],"bead_default":14.5,"overlap":5,"depth":70,"combo":112},
  "EXPROF_PROFECTA_S571_70": {"frame":60,"mullion":74,"sash":74,"bead":"S358","bead_default":20,"overlap":8,"depth":70}
}
```

## 4. Геометрическая модель

### 4.1 Проём и рама
- `opening_poly = [(0,0),(OW,0),(OW,OH),(0,OH)]`
- Рама: `frame_outer`/`frame_inner` с учётом `S`/`SH`/`addons` и `system`, 4 стыка 45° по диагоналям углов.

### 4.2 Сетка и импосты (со сплошным направлением)
- `grid: x0=frame_left+FW, x1=frame_right-FW, y0=frame_bottom+FH, y1=frame_top-FH`
- `ΣV=(cols-1)·mw, ΣH=(rows-1)·mh`, `cell_w=(grid_w-ΣV)/cols`, `cell_h=(grid_h-ΣH)/rows`, замыкание `<1e-6`.
- **Сплошной импост** `mullion.continuous`:
  - `auto` — сплошной по **наименьшей стороне** `grid_w vs grid_h`:
    - `grid_w +1e-9 < grid_h` (ширина меньше) → **горизонталь сплошная** (`mullions_h` на всю ширину `x0..x1`, цельными),
    - `grid_h +1e-9 < grid_w` → **вертикаль сплошная** (`mullions_v` на всю высоту `y0..y1`),
    - равенство (квадрат с допуском 1e-9) → `vertical` (tie-break).
  - `vertical` — все `cols-1` вертикалей цельные `y0..y1`, каждая горизонталь режется на `cols` сегментов между вертикалями (зазор `=mw`).
  - `horizontal` — все `rows-1` горизонталей цельные `x0..x1`, каждая вертикаль режется на `rows` сегментов (зазор `=mh`).
  - Делёный импост **без пересечения** со сплошным (разрыв равен толщине сплошного). При `cols=1` или `rows=1` — соответственно 1 сегмент на всю сторону.
  - В модели: `mullion_continuous ∈ {vertical,horizontal}`, `mullions_v` — список сегментов-четыреугольников (если vertical continuous → `cols-1` шт., иначе `(cols-1)*rows`), `mullions_h` — аналогично.
  - Переключатель мастера: радио `Авто / Вертикаль / Горизонталь` пишет `mullion.continuous` в `params.json` и переопределяет `auto`.

### 4.3 Створки
- Для `FIX` — пропуск; иначе наружный `[(x1-SO…)]`, внутренний `[(x1-SO+SW…)]`, 4 стыка 45°.
- Индикация открывания **по видимому габариту ячейки** `cell (x1,y1)-(x2,y2)`:
  - `TURN` → `((x1,y1)-(x2,ymid)), ((x1,y2)-(x2,ymid))`
  - `TILT` → `((x1,y1)-(xmid,y2)), ((x2,y1)-(xmid,y2))`
  - `TURN_TILT` → 4 линии (совмещение)
- `view=INSIDE` — зеркало и обрезка рамы/импостов створкой (`outer_rect`).

### 4.4 Подставочник, доборы, штапик и заполнение
- `sill_poly = [(S,S),(OW-S,S),(OW-S,S+SH),(S,S+SH)]`, зеркалится для `INSIDE`.
- Доборы — прямоугольники снаружи рамы, учитываются в габаритах `overall_*`.
- **Штапик** `bw = bead.width` (из системы или `params`, default из системы) — единый для системы:
  - рама: 4 полосы вдоль `frame_inner` (низ `y1..y1+bw`, верх `y2-bw..y2`, лево `x1..x1+bw` без углов, право `x2-bw..x2`);
  - вертикальный импост: для каждого сегмента — 2 полосы слева/справа `bw × (y2-y1)`;
  - горизонтальный импост: 2 полосы снизу/сверху;
  - створка: 4 полосы внутрь `inner_rect` (аналогично раме).
  - Если `bw == 0` — штапик отключён (`bead_polys == []`). Если `bw` слишком велик для ячейки (`w <=2·bw` или `h <=2·bw`) — полосы не генерируются для этой ячейки. Для `INSIDE` полосы рамы/импостов обрезаются створкой по `outer_rect` (как рама).
- **Заполнение** — справочный контур стеклопакета, **непечатный слой `Заполнение` (`plot=0`, скрытый)**:
  - Для каждой ячейки `cols×rows` строится `filling_poly = (cell_rect or inner_rect) inset by bw` (отступ `bw` внутрь от `cell` или от `inner_rect` створки). Контур `LWPOLYLINE` на `Заполнение`.
  - В левом нижнем углу заполнения текст размера `WхH` (кириллица `х`, целые без `.0`, `round 1` если дробь) в точке `(fx1+8, fy1+8)` `h=16` на том же слое `Заполнение`, `halign=0,valign=0`. Пример `358.3х575`, `570х570`.
  - Зеркалится для `INSIDE` вместе с геометрией. Не считается в `primitives_count`.
  - В модели: `fillings[] = {cell, rect=(fx1,fy1,fx2,fy2), poly, w, h, text, pos}`, `filling_polys`, `filling_texts`.

## 5. DXF-документ

- **Версия** `R2013`, `DWGCODEPAGE ANSI_1251`, `encoding cp1251`, `LTSCALE 25, CELTSCALE 1` (если шаблон — сохраняет его, иначе 25).
- **Слои**: `Окна` 7 `Continuous`, `Заполнение` 6 `Continuous` `plot=0` (непечатный, скрытый), `Штриховые` 7 `GOST2.303 4` вес 9, `Размеры` 3, `Текст` 7, `Основной` 7; типы линий копируются из шаблона, блоки стрелок `_Dot*` импортируются.
- **Стили**: `WindowStyle`/`Основной стиль` `Arial.ttf`, `Основной стиль (для надписей)` (атрибуты/площадь/заполнение `h=30/16`), `dimstyle` — см. §6.

## 6. Блок, атрибуты, площадь

- **Имя блока**: `"{object} {window_name} {W}х{H} {Снаружи|Изнутри}"` (кириллица `х`), ≤255.
- **Атрибуты (6, слой `Текст`, стиль `Основной стиль (для надписей)`, `h=30`)**:
  `OBJECT` (объект), `WINDOW` (`ОК-1/1 шт.`), `COLOR` (`RAL8017/RAL9016` без пробелов), `GLAZING` (`Заполнение СПД…`), `SIZE` (`…х…`), `GRID` (`…х…`).
  `x=0, y_start=OH+60, step 50`, `OBJECT` самый верхний (`y_start+(n-1)·step`), `GRID` самый нижний.
- **Площадь**: `TEXT S=… м²` в `(OW, OH+60)` `TOP_RIGHT`, `h=30`, слой `Текст`, выровнена по Y с `GRID`.
- **Вставка**: `ModelSpace (0,0)`, слой `Окна`, `1:1`.
- **Примитивы**: `opening 1 + frame 2+4 + mullions (сегментированные) + sill + addons + bead_polys (4 +2·V+2·H +4·S) + sashes (inner+mitres+indicators, outer только INSIDE) + 6 attdef` (+ справочные `filling_polys` на `Заполнение` не считаются).

## 7. Размерные цепочки (три уровня, масштаб 4)

- **Слой** `Размеры`, **стили**: `Основной стиль` (1-я и 3-я) и `Основной стиль с точками` (2-я). Оба `dimtxt 8, dimasz 6, dimscale 4, dimexe 3, dimexo 2.5, dimgap 3`, `dimtxsty Основной стиль`.
- **Отступы**: горизонт `-80/-160/-240`, вертикаль `+80/+160/+240` (было 30/60/90 → 80/160/240, масштаб 4).

| Уровень | Горизонт (y) | Вертик. (x) | Что измеряет | Стиль |
|---|---|---|---|---|
| 1-я детализация | `-80` | `OW+80` | ячейки (`frame_left + centres + frame_right`) + боковые доборы отдельно | Основной |
| 2-я габарит окна | `-160` | `OW+160` | `overall_left → overall_right` / `frame_bottom → frame_top` **+ подставочник `+80` внутрь (с точками) + верх. добор** | **с точками** |
| 3-я проём | `-240` | `OW+240` | `0 → OW` / `0 → OH` | Основной |
| **Швы** (в одну линию со 2-й) | **`-160`** | **`OW+160`** | гориз. `0→overall_left`, `overall_right→OW` (y=0); вертик. `0→horiz_ref_y`, `overall_top→OH` (x=OW) | Основной |

- `horiz_ref_y = sill_bottom` если есть подставочник, иначе `frame_bottom`.
- `vert_ref_x = overall_right` если есть боковые доборы, иначе `frame_right`.
- Вторая цепочка — **единственная** со стилем с точками; швы — обычным, но коллинеарны со второй.

## 8. Условные обозначения открывания

- Слой `Штриховые`, `linetype_scale 25`.
- `INSIDE` → `Continuous`, `OUTSIDE` → `ByLayer` (`GOST2.303 4`).

## 9. Конвертация DWG и файлы

- Поиск ODA: `C:\Program Files\ODA\ODAFileConverter*`, `… (x86)`, `%LOCALAPPDATA%\Microsoft\WinGet\Packages\ODA.ODAFileConverter*`, `PATH`.
- Команда: `ODAFileConverter <out> <out> ACAD2013 DWG 0 1 <имя.dxf>` → при успехе `*.dwg` создан → **DXF удаляется** (`ОК-1.dxf`, `ОК-1_INSIDE.dxf`, `Все_примеры.dxf`). При `ODA не найден` — DXF остаётся.
- **Блокировка Windows** (`Permission denied 13` при открытом файле в проводнике/AutoCAD): перед `saveas` пытаться `unlink()`, при занятости — сохранять `*_new.dxf` с подсказкой, не падать с traceback; аналогично при `unlink()` после DWG.

## 10. Сборка всех примеров

- `make_examples()` — 12 шт (6×2): `ОК-1.1 900×900 1×1` … `ОК-УЗ 1200×2100 2×3`, нечётные `OUTSIDE`, чётные `INSIDE`, доборы/профили варьируются; **3 системы**: `01-04 ABSTRACT`, `05-08 REHAU GRAZIO 70`, `09-12 EXPROF Profecta S571`; диверсификация `continuous` (3×2 `auto`, 3×3 `vertical`, 4×3 `horizontal`, 8×4 `auto`) и `bead` (`20/14.5/30`).
- `export_all_to_one(models, output, template)`:
  - создаёт doc (с шаблоном), обеспечивает слои/стили (`dimscale 4` для обоих стилей) и слой `Заполнение` `plot=0`,
  - для каждого `build_window_model` → уникальное имя `"{obj} {wn} {size} {grid} {view} #NN"`,
  - раскладка `3500×3500` (`cols_on_sheet=3`),
  - через `tempfile + export_to_dxf + Importer.import_blocks`,
  - вставка `BlockRef` + `ATTRIB` (`h=30`, `Текст`) + подпись `MText` под блоком,
  - `saveas` с обработкой занятости, **конвертация DWG + удаление DXF**, возврат фактического пути, печать перечня.
- Вызывается как из `generate_examples.py`, так и из `window_export.py` (последний — только если `sys.argv` не содержит `unittest`/`test_window_export`).

## 11. Тесты

`test_window_export.py` — 17 сценариев: версия/кодировка, `auditor.errors`, замыкание, слои `Окна 7/195, Штриховые GOST2.303 4 0.09, Заполнение 6 plot0, Размеры, Текст`, стили, `dimstyle` ∈ { основной, с точками }, `GRID 3х2` без `?` (`WindowStyle`/`Основной`), точка вставки, имя блока, ODA-ветка, пересчёт, `1×1`/`8×4` (с учётом сегментирования сплошного импоста), `INSIDE/OUTSIDE`, `sill/addons` + **2 новых**: `16 сплошной импост` (auto по наименьшей стороне, ручной переключатель, разрыв, алиасы, валидация) и `17 штапик` (default из системы, алиасы `shtapik`/`bead_width`, `>=0`/`<=FW`, полигоны, DXF) + проверка `Заполнение` (контур + текст `WхH` в левом нижнем углу, `plot=0`).

## 12. Нефункциональные требования

- Точность геометрии `1e-9` для обрезки, `1e-6` для замыкания.
- Совместимость: AutoCAD 2016/2023, `ezdxf` чтение DWG до `R2000` — для `Шаблон.dwg R2013` требовать `Шаблон.dxf`.
- Кодировка кириллицы — только `cp1251`.
- Повторяемость: `params.json` по умолчанию, `output/` создаётся.

## 13. История правок (сжато)

- **2026-09-20**: 12 INSERT, 2-я цепочка -120/1620, `view OUTSIDE`.
- **2026-09-20 18:00**: `Все_примеры` в DWG, стиль для надписей, `dimscale 4`, доборы уменьшают раму.
- **2026-09-21 5 критериев**: `dimscale 4`, базы `80/160/240`, `Штриховые GOST 4 0.09`, `INSIDE Continuous / OUTSIDE ByLayer`, `6+6` видов, перечень.
- **2026-09-21 (сегодня)**: площадь `Y=GRID` (`OH+60`), 2-я цепочка — стиль с точками, швы/подставочник наружу/внутрь, `Все_примеры.dxf→.dwg` при ODA, обработка `PermissionError` (`_new`), выравнивание масштаба 2-й цепочки (4) и швы в одну линию со 2-й.
- **2026-09-21 02:30**: сплошной импост (`mullion.continuous = auto|vertical|horizontal`, auto по наименьшей стороне `grid_w vs grid_h`, квадрат → vertical, делёный режется с разрывом, переключатель мастера `Авто/Вертикаль/Горизонталь`) + штапик (`bead`/`shtapik` `width` default из системы, единый для рамы/импостов/створок, `bead_polys` 4+2·V+2·H+4·S, `INSIDE` обрезка).
- **2026-09-21 04:50**: системы `profiles.json` (`ABSTRACT 60/80/25`, `REHAU GRAZIO 70 63/76/14.5/5`, `EXPROF Profecta S571 70 60/74/20/8`, переключатель `params.system`) + справочное заполнение (`Заполнение` слой `plot=0`, контур `LWPOLYLINE` в каждой ячейке `cols×rows` + текст `WхH` 16 мм в левом нижнем углу `(fx1+8,fy1+8)`).

## 14. Открытые вопросы

- Вес `GOST2.303` — 0.09 vs иные; тип `4` vs `6` (сейчас `4`).
- Нужен ли `LTSCALE` из шаблона vs форсированные 25 (сейчас сохраняется шаблонный, иначе 25).
